## 🎯 Blog (in Bangla)

### ১. TypeScript-এ Interface ও Type-এর পার্থক্য

TypeScript-এ **interface** এবং **type** দিয়ে object-এর type/structure বানানো যায়, তবে কিছু পার্থক্য আছে:

**>>Extends/implements:** **interface** সহজে extend ও implement করা যায়, ক্লাসে implements ব্যবহার করা হয়;
                        **type** -ও extend/intersect করা যায়, তবে একটু কম সুবিধাজনক।

**>>Declaration merging:** একই নামের **interface** বারবার declare করলে গুণাবলী নিজে নিজেই যোগ হয়; **type**-এর ক্ষেত্রে তা হয় না।

**>>Primitives-aliasing:** **type** দিয়ে primitive type, union, tuple ইত্যাদির alias বানানো যায়, কিন্তু **interface** শুধু object-এর জন্য।

**>>Advanced types:** **type**-এ conditional, mapped, union ইত্যাদি complex type define করা যায়, **interface**-এ করা যায় না।

 সংক্ষেপে,interface বেশি উপযোগী object structure বা class contract বোঝাতে, এবং type alias বেশি উপযোগী advanced type বা union/primitive-alias-এর জন্য |


### ২. any, unknown, never – পার্থক্য

TypeScript-এ any, unknown এবং never—তিনটি টাইপ কিন্তু কাজ ও নিরাপত্তায় আলাদা:

any: যেকোনো ধরনের value নিতে পারে; কোনো টাইপ চেক হয় না, ফলে টাইপ নিরাপত্তা থাকে না।

unknown: যেকোনো value নিতে পারে, তবে সরাসরি ব্যবহার করা যাবে না—কোনো অপারেশন বা এসাইনমেন্টের আগে টাইপ চেক করতে হয়। এটি নিরাপত্তা দেয়।

never: এমন ফাংশন বা ভেরিয়েবল বোঝাতে হয়, যা কখনো কোনো value রিটার্ন করবে না (যেমন, চিরকাল চলতে থাকা বা সবসময় error দেয় এমন ফাংশন)। এটি ধরিয়ে দেয় ফাংশন কোনোভাবেই return করবে না।

সংক্ষেপে,

any: সবচেয়ে কম নিরাপদ, সবকিছুই হতে পারে।

unknown: অনেকটা any-এর মতো, তবে টাইপ চেক ছাড়া ব্যবহার করা যায় না।

never: কোনোসময়ই কোনো value/return হয় না (যেমন, throw new Error-এর ফাংশন)
